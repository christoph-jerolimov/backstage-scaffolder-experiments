apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  namespace: backstage-plugin-templates
  name: all-backstage-templates
  title: backstage-plugin-templates - all backstage templates
spec:
  targets:
    - ./create-backstage-app/template.yaml
    - ./create-backstage-plugin/template.yaml
